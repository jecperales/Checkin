import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.grupopissa.checkin',
  appName: 'Checkin',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
